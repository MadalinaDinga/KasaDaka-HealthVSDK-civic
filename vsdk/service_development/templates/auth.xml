{% extends "base.xml" %}
{% block content%}
<form id="auth_form">
	<field name="username_field" type="number">
		<prompt>
            <audio src="{{ request_username_voice_label }}"/>
		</prompt>

       {# when there has been input, save the corresponding username in the given variable and then ask for the password #}
       <filled>
            <prompt>
                <audio src="{{ thank_you_voice_label }}"/>
		    </prompt>
            <assign name="username" expr="username_field"/>
      </filled>
    </field>

    <field name="password_field" type="number">
            <prompt>
                <audio src="{{ request_password_voice_label }}"/>
            </prompt>

       {# when there has been input, save the corresponding password in the given variable #}
       <filled>
            <prompt>
                <audio src="{{ thank_you_voice_label }}"/>
		    </prompt>
            <assign name="pass" expr="password_field"/>
        <goto next="#submit_form"/>
      </filled>
     </field>
</form>

<form id="submit_form">
    <block>
        {# include the variables in the dictionary 'pass_on_variables' as POST variables #}
        <submit next="{{ redirect_url }}" enctype="multipart/form-data" method="post" namelist="username pass"/>
    </block>
</form>

{% endblock %}
